export interface Bus {
    id?: number;
    name: string;
    route: string;
}
export interface BusType {
    id: number;
    name: string;
}

export interface Location {
    id: number;
    name: string;
}